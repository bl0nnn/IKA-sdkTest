# HOW TO USE
clone the repository and do npm install.
after that just create 2 files in config/ dir: wallets.json and presignatures .json. modify the .env file as shown in .env.example and adjust index.ts to sign any payment transaction you want .

